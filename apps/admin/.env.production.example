# Production API URL (TASK-004: HTTPS only).
# NOT committed — copy this file to .env.production (gitignored) or inject
# NEXT_PUBLIC_API_URL as a build/run-time secret in CI/CD. It is inlined at
# build time (`next build`). See docs/tls.md.
NEXT_PUBLIC_API_URL=https://paymentswitch.example.com
