# Production API base URL (AWS EC2, Docker Compose behind nginx on port 80)
# Used by `next build` / `next start` (NODE_ENV=production).
# Local development uses apps/<app>/.env.local instead (gitignored) -> http://localhost
NEXT_PUBLIC_API_URL=http://16.171.58.173