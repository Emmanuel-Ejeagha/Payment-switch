apiVersion: 1

providers:
  - name: PaymentSwitch
    orgId: 1
    folder: PaymentSwitch
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards