name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  REGISTRY: ghcr.io
  NAMESPACE: payment-switch
  # github.repository is "owner/repo" (owner and repo names are already lowercase).
  # The previous `lower(...)` is not a valid GitHub Actions expression and
  # produced an empty owner, silently corrupting image paths and deploy refs.
  IMAGE_NAMESPACE: ${{ github.repository }}

jobs:
  build-test:
    name: Build & Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj', '**/Directory.Packages.props', '**/packages.lock.json') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      - name: Restore dependencies
        run: dotnet restore PaymentSwitch.slnx

      - name: Verify formatting
        run: dotnet format PaymentSwitch.slnx --verify-no-changes --no-restore

      - name: Build solution
        run: dotnet build PaymentSwitch.slnx --configuration Release --no-restore

      - name: Run unit tests with coverage
        run: |
          # Excludes the Testcontainers integration suites (which need Docker and
          # are slow); they run serially in the dedicated 'integration' job below.
          dotnet test PaymentSwitch.slnx --configuration Release --no-build \
            --filter "FullyQualifiedName!~API.IntegrationTests" --verbosity normal \
            --collect:"XPlat Code Coverage" \
            --results-directory TestResults

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: TestResults/

  integration:
    name: Integration Tests
    if: github.event_name == 'pull_request' || github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj', '**/Directory.Packages.props', '**/packages.lock.json') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      - name: Restore dependencies
        run: dotnet restore PaymentSwitch.slnx

      - name: Build solution
        run: dotnet build PaymentSwitch.slnx --configuration Release --no-restore

      - name: Pre-pull Postgres image
        run: docker pull postgres:16-alpine

      # Each project runs in its own step so Testcontainers containers are not
      # spun up concurrently (parallel container startup can exceed the Docker
      # connection limit and cause flaky named-pipe timeouts).
      - name: Integration tests - Identity
        run: dotnet test tests/Integration/Identity.API.IntegrationTests --configuration Release --no-build
      - name: Integration tests - Merchant
        run: dotnet test tests/Integration/Merchant.API.IntegrationTests --configuration Release --no-build
      - name: Integration tests - Payment
        run: dotnet test tests/Integration/Payment.API.IntegrationTests --configuration Release --no-build
      - name: Integration tests - Ledger
        run: dotnet test tests/Integration/Ledger.API.IntegrationTests --configuration Release --no-build
      - name: Integration tests - Notification
        run: dotnet test tests/Integration/Notification.API.IntegrationTests --configuration Release --no-build
      - name: Integration tests - Settlement
        run: dotnet test tests/Integration/Settlement.API.IntegrationTests --configuration Release --no-build

  frontend:
    name: Frontend Build & Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install workspace dependencies
        run: npm ci

      - name: Audit dependencies
        run: npm audit --audit-level=high

      - name: Typecheck admin
        run: npm run typecheck --workspace=admin

      - name: Typecheck merchant
        run: npm run typecheck --workspace=merchant

      - name: Lint admin
        run: npm run lint --workspace=admin

      - name: Lint merchant
        run: npm run lint --workspace=merchant

      - name: Build admin
        run: npm run build --workspace=admin

      - name: Build merchant
        run: npm run build --workspace=merchant

  docker:
    name: Docker Build & Push
    needs: [build-test, integration]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service:
          - identity
          - merchant
          - payment
          - ledger
          - notification
          - settlement
    steps:
      - uses: actions/checkout@v4

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: src/Services/${{ matrix.service }}/${{ matrix.service }}.API/Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAMESPACE }}/${{ matrix.service }}-api:${{ github.sha }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAMESPACE }}/${{ matrix.service }}-api:latest

      - name: Scan image for vulnerabilities
        uses: aquasecurity/trivy-action@0.24.0
        with:
          image-ref: ${{ env.REGISTRY }}/${{ env.IMAGE_NAMESPACE }}/${{ matrix.service }}-api:${{ github.sha }}
          severity: HIGH,CRITICAL
          format: sarif
          output: trivy-${{ matrix.service }}.sarif
          exit-code: '1'
          ignore-unfixed: true

      - name: Upload Trivy scan results
        uses: github/codeql-action/upload-sarif@v3
        if: success() || failure()
        with:
          sarif_file: trivy-${{ matrix.service }}.sarif

  deploy:
    name: Deploy to Kubernetes (production)
    needs: docker
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4

      - name: Setup kubectl
        uses: azure/setup-kubectl@v3

      - name: Configure kubeconfig
        run: |
          mkdir -p $HOME/.kube
          echo "${{ secrets.KUBE_CONFIG }}" | base64 --decode > $HOME/.kube/config

      - name: Record previous image (for rollback)
        run: |
          services=("identity" "merchant" "payment" "ledger" "notification" "settlement")
          for svc in "${services[@]}"; do
            kubectl get deployment/${svc}-api --namespace ${{ env.NAMESPACE }} \
              -o jsonpath="{.spec.template.spec.containers[0].image}" | tee /tmp/${svc}-previous-image.txt
          done

      - name: Update deployments with new images
        run: |
          services=("identity" "merchant" "payment" "ledger" "notification" "settlement")
          for svc in "${services[@]}"; do
            kubectl set image deployment/${svc}-api \
              ${svc}-api=${{ env.REGISTRY }}/${{ env.IMAGE_NAMESPACE }}/${svc}-api:${{ github.sha }} \
              --namespace ${{ env.NAMESPACE }}
          done

      - name: Verify rollout status
        run: |
          kubectl rollout status deployment/identity-api --namespace ${{ env.NAMESPACE }} --timeout=120s
          kubectl rollout status deployment/merchant-api --namespace ${{ env.NAMESPACE }} --timeout=120s
          kubectl rollout status deployment/payment-api --namespace ${{ env.NAMESPACE }} --timeout=120s
          kubectl rollout status deployment/ledger-api --namespace ${{ env.NAMESPACE }} --timeout=120s
          kubectl rollout status deployment/notification-api --namespace ${{ env.NAMESPACE }} --timeout=120s
          kubectl rollout status deployment/settlement-api --namespace ${{ env.NAMESPACE }} --timeout=120s

      - name: Rollback on rollout failure
        if: failure()
        run: |
          services=("identity" "merchant" "payment" "ledger" "notification" "settlement")
          for svc in "${services[@]}"; do
            PREV=$(cat /tmp/${svc}-previous-image.txt)
            kubectl set image deployment/${svc}-api ${svc}-api=$PREV --namespace ${{ env.NAMESPACE }}
          done
          echo "Deployment failed; images reverted to previously-recorded versions."